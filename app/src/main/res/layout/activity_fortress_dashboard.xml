<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="#1A1A2E">

    <!-- Tab bar -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="56dp"
        android:orientation="horizontal"
        android:background="#111128">

        <Button
            android:id="@+id/btnTabDashboard"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:text="📊\nDashboard"
            android:textSize="11sp"
            android:textColor="#FFFFFF"
            android:background="#2A2A4A" />

        <Button
            android:id="@+id/btnTabApps"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:text="📱\nBlocked"
            android:textSize="11sp"
            android:textColor="#FFFFFF"
            android:background="#2A2A4A" />

        <Button
            android:id="@+id/btnTabDns"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:text="🌐\nDNS"
            android:textSize="11sp"
            android:textColor="#FFFFFF"
            android:background="#2A2A4A" />

        <Button
            android:id="@+id/btnTabSettings"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:text="⚙️\nSettings"
            android:textSize="11sp"
            android:textColor="#FFFFFF"
            android:background="#2A2A4A" />

    </LinearLayout>

    <!-- Fragment container — fragments shown/hidden here -->
    <FrameLayout
        android:id="@+id/fragmentContainer"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

</LinearLayout>